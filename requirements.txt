# 影片自動化處理系統 - Python 相依套件
# 版本: v2.2 (完整優化版)
# 最後更新: 2025-01-15

# ======================================
# 環境變數管理
# ======================================

# .env 檔案載入
python-dotenv>=1.0.0,<2.0.0

# ======================================
# 核心影片處理套件
# ======================================

# 影片下載和處理 - 使用最新版本確保支援最多平台
yt-dlp>=2024.1.0,<2025.0.0

# ======================================
# 雲端儲存服務
# ======================================

# AWS SDK (用於 Cloudflare R2 S3 相容 API)
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# ======================================
# AI 和機器學習服務
# ======================================

# OpenAI API 客戶端
openai>=1.10.0,<2.0.0

# ======================================
# HTTP 和網路請求
# ======================================

# HTTP 請求庫 (用於 Notion API 和其他 REST API)
requests>=2.31.0,<3.0.0

# URL 解析和網路工具
urllib3>=1.26.18,<3.0.0

# SSL/TLS 證書處理
certifi>=2023.11.17

# ======================================
# 重試和錯誤處理
# ======================================

# 重試機制庫
tenacity>=8.2.3,<9.0.0

# ======================================
# 資料處理和格式化
# ======================================

# 日期時間處理
python-dateutil>=2.8.2,<3.0.0

# 時區處理
pytz>=2023.3,<2025.0

# JSON 處理增強
simplejson>=3.19.0,<4.0.0

# ======================================
# 檔案和路徑處理
# ======================================

# 路徑匹配模式
pathspec>=0.11.0,<1.0.0

# 向後相容性支援
pathlib2>=2.3.7,<3.0.0; python_version < "3.4"

# ======================================
# 加密和安全
# ======================================

# 加密庫 (boto3 和其他套件的依賴)
cryptography>=41.0.0,<43.0.0

# ======================================
# 系統監控和效能 (可選)
# ======================================

# 系統和行程監控 (用於效能統計)
psutil>=5.9.0,<6.0.0; platform_system != "Windows"

# ======================================
# 開發和測試工具 (僅開發環境)
# ======================================

# 型別檢查工具 (開發用，可選)
# typing-extensions>=4.8.0,<5.0.0

# 測試框架 (開發用，可選)
# pytest>=7.4.0,<8.0.0
# pytest-asyncio>=0.21.0,<1.0.0

# 程式碼格式化 (開發用，可選)
# black>=23.0.0,<24.0.0
# isort>=5.12.0,<6.0.0

# ======================================
# 結構化日誌 (進階功能，可選)
# ======================================

# 結構化日誌庫 (如果需要更進階的日誌功能)
# structlog>=23.2.0,<24.0.0

# ======================================
# 版本相容性說明
# ======================================

# Python 版本要求: >= 3.8
# 建議使用 Python 3.11 或更新版本以獲得最佳效能

# 重要版本鎖定原因:
# - yt-dlp: 經常更新以支援新平台和修復問題，使用較新版本
# - openai: API 介面相對穩定，鎖定主要版本避免破壞性變更
# - boto3/botocore: AWS SDK 更新頻繁，鎖定主要版本確保穩定性
# - requests: 核心網路庫，使用穩定版本
# - tenacity: 重試機制核心，版本穩定
# - cryptography: 安全關鍵，使用經過測試的版本範圍

# 安裝指令:
# pip install -r requirements.txt

# 升級指令:
# pip install -r requirements.txt --upgrade

# 檢查相依性:
# pip check

# 生成鎖定版本 (用於生產環境):
# pip freeze > requirements-lock.txt